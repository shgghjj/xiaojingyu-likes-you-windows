# 小鲸鱼喜欢你 Windows 版 · 开发日志

## 2026-08-04 夜间 · 阶段0完成

### 状态
- **阶段0（环境+骨架）：完成** ✅
- **阶段1（核心逻辑迁移）：进行中**

### 已完成
1. 创建项目：`C:\Users\xiagu\Documents\Codex\2026-08-01\q\work\XiaojingyuDesktop`
2. 技术栈：**Kotlin JVM + Compose Desktop (Material3) + OkHttp + kotlinx-serialization**
   - Kotlin 2.0.21，Compose 1.7.3，Gradle 8.9（复用手机版 wrapper）
3. 主入口 `Main.kt`：1100x720 窗口，暗色主题（0xFF0A0A0E 背景 + 0xFF6EC6F0 青色）
4. `build.gradle.kts`：nativeDistributions 配置（MSI 打包）
5. **BUILD SUCCESSFUL**（3m44s），窗口可启动（java 进程验证过）

### 修复的问题
- `TargetFormat.Dmg` 在 Windows 上不可用 → 只留 Msi + 加 import

### 环境备忘
- JAVA_HOME: `C:\Users\xiagu\Documents\Codex\2026-08-01\q\work\toolchain\jdk17\jdk-17.0.20+8`
- GRADLE_USER_HOME: `C:\Users\xiagu\Documents\Codex\2026-08-01\q\work\toolchain\gradle-home`
- 构建命令（workdir=XiaojingyuDesktop）:
  `$env:JAVA_HOME=...; $env:GRADLE_USER_HOME=...; .\gradlew.bat build --no-daemon --console=plain`
- 运行命令: `.\gradlew.bat run --no-daemon`

### 下一步（阶段1）
- 从手机版复制纯 Kotlin 核心逻辑：
  - GirlfriendPromptBuilder.kt（零 Android 依赖）
  - GirlfriendMemoryService.kt（零依赖）
  - GirlfriendDynamicContext.kt（零依赖）
  - GirlfriendMemory.kt（1个Android依赖，需改）
  - JailbreakLibrary.kt（1个依赖，需改）
  - SecureGirlfriendStorage.kt（3个依赖，需改）
  - GeminiVisionClient.kt（1个依赖，需改）
- 编写桌面版数据层：ConfigStore（API配置）、ChatStore（聊天存储）

## 2026-08-05 凌晨 · 阶段1核心迁移完成

### 状态
- 阶段1（核心逻辑迁移）：**完成** ✅
- 阶段2（桌面UI）：进行中

### 已完成
1. 复制手机版纯 Kotlin 核心逻辑到桌面项目（改 package 为 com.xiaojingyu.app.*）：
   - GirlfriendPromptBuilder.kt（提示词，零改动）
   - GirlfriendMemoryService.kt（记忆整理，改 LlmClient）
   - GirlfriendDynamicContext.kt（动态上下文）
   - GirlfriendMemory.kt（改 File 目录构造）
   - JailbreakLibrary.kt（改 classloader 读资源）
   - SecureGirlfriendStorage.kt（重写：AES-GCM + 本地密钥文件）
   - GeminiVisionClient.kt（Base64 换 java.util）
2. 模型类迁移：ApiConfiguration/ChatMessage/StreamEvent/Result/PromptMessage（+ effectiveBaseUrl 扩展）
3. 新建 LlmClient.kt：流式+非流式 chat completions（OkHttp+SSE）
4. 新建 ConfigStore.kt：API/设置加密存储
5. 新建 ChatStore.kt：聊天记录加密存储
6. 内置破甲词库 3 个 txt 复制到 resources

### 构建状态
- compileKotlin BUILD SUCCESSFUL

### 下一步
- 阶段2：桌面 UI（主界面：Live2D占位 + 聊天 + 设置抽屉 + 托盘）

## 2026-08-05 凌晨 · 阶段2完成：桌面UI + 首版可安装包

### 状态
- 阶段2（桌面UI）：**完成** ✅
- 首个可安装包：**小鲸鱼喜欢你-Windows版-v0.1.0.msi（92MB）** 已放桌面/Windows牛逼/

### 已完成
1. AppState.kt：桌面版聊天状态管理（发送/流式/记忆/无聊值/配置）
2. Main.kt 重构：完整主界面
   - 左栏：白音头像+无聊值进度条+设置入口
   - 中间：聊天区（气泡+流式+输入框，暗色终端风）
   - 设置对话框：API Key/模型/主动联系/文件读取开关（含确认弹窗）/Gemini Key
3. StructuredReplyParserDesktop：JSON 剥离+思考清洗
4. MSI 安装包打包成功（WiX 手动下载修复）

### 修复
- WiX 从 GitHub 慢/损坏 → 官方 WebClient 下载 34MB 成功
- ChatPanel weight 作用域错误

### 环境备忘（重要）
- WiX 缓存：gradle-home/compose-jb/wix311.zip（已就位，下次打包不需要重下）
- MSI 输出：build/compose/binaries/main/msi/
- 可运行目录：build/compose/binaries/main/app/（未生成，因依赖 unzipWix）

### 下一步
- 阶段3：Live2D + 语音（TTS）
- 阶段4：文件沙盒 + 行动账本 + 回收站
- 阶段5：编码 Agent（命令/代码/Git）
- 阶段6：插件系统

## 2026-08-05 01:00 · 首版可运行验证通过

### 重要成果
- **小鲸鱼喜欢你 Windows 版 v0.1.0 首次成功运行！**
- 窗口标题、进程稳定（20秒+无崩溃）
- 产物：
  - 免安装版：桌面/Windows牛逼/小鲸鱼喜欢你-Windows版-v0.1.0-免安装.zip（91MB，解压即用）
  - MSI：小鲸鱼喜欢你-Windows版-v0.1.0.msi（92MB，安装报1603，WiX RemoveFoldersEx 已知问题，暂不修复，用免安装版）

### 修复
- Main.kt 重写时丢了 fun main() → 补回（jpackage 报"找不到main方法"）
- MSI 1603：Compose Desktop WiX 打包已知 bug，放弃 MSI 优先免安装版

### 下一步
- 阶段3：Live2D（WebView2 内嵌手机版 index.html）+ TTS 语音
- 阶段4：文件沙盒 + 行动账本 + 回收站
- 阶段5：编码 Agent
- 阶段6：插件系统

## 2026-08-05 02:00 · 阶段3(TTS)+阶段4(沙盒)完成

### 已完成
1. TtsSpeaker.kt：Windows SAPI 语音（PowerShell System.Speech 调用），实测可发声
   - 支持语速/音量调节，AppConfig 加 ttsEnabled 开关，回复自动朗读
2. ActionLedger.kt：行动账本
   - 所有操作如实记录（时间/类型/原路径/新路径/备份位置/可恢复）
   - 文件修改前自动备份到 data/backups
   - 一键 restoreAll() 恢复所有操作
3. FileSandbox.kt：文件沙盒管理器
   - 沙盒目录：文档/小鲸鱼喜欢你/沙盒
   - 读：全盘文本（受 fileReadEnabled 开关+黑名单控制）
   - 写/删/藏：沙盒内自由，外部需授权目录
   - 黑名单：.ssh/浏览器密码库/系统配置等永远禁读
   - 全部操作走账本

### 构建状态
- compileKotlin BUILD SUCCESSFUL

### 下一步
- 阶段5：编码 Agent（命令执行器 CommandExecutor + Git 操作 + 确认制）
- 阶段6：插件系统 + 预留接口
- 阶段3b：Live2D（WebView2 内嵌）

## 2026-08-05 03:00 · 阶段5+6完成：编码Agent + 插件系统

### 已完成
1. CommandExecutor.kt：命令执行器（cmd /c，超时终止，输出捕获，账本记录）
2. GitOperator.kt：git 封装（status/log/branch/diff/add/commit/push/pull/分支操作）
3. PluginSystem.kt：插件系统
   - XiaojingyuPlugin 接口 + PluginContext（账本/沙盒/配置/命令）
   - PluginRegistry（注册/启用/禁用）
   - GameSessionPlugin 预留接口（联机游戏：我的世界等）
4. AppState 扩展：sandbox/ledger/commandExecutor/gitOperator/插件注册（含 DemoPlugin 便签插件）
5. UI 右栏：行动账本（含一键恢复按钮）+ 沙盒浏览（含隐藏文件识别 🔒）

### 验证
- 应用运行正常，窗口标题"小鲸鱼喜欢你"
- 沙盒目录已自动创建：文档/小鲸鱼喜欢你/沙盒
- 免安装版已更新：桌面/Windows牛逼/（91MB zip）

### 剩余
- 阶段3b：Live2D（WebView2 内嵌手机版 index.html + 模型文件）
- 阶段7：安全打磨 + 打包（MSI 问题待解）
- 主动消息/无聊值后台（Windows 定时器版）

### 下一步计划
1. Live2D WebView2 接入（最重头）
2. 无聊值后台主动消息（Java Timer/协程循环）
3. 破甲词库 UI 选择器 + 记忆管理面板

## 2026-08-05 04:00 · 无聊值后台 + Live2D 舞台（浏览器版）

### 已完成
1. AppState 无聊值循环（Windows 无后台限制）：
   - 每60秒检查，3分钟+1点
   - 达到阈值+主动联系开启+非安静时段 → 触发主动消息
   - 主动消息低token调用，生成后写入聊天+TTS朗读+回调通知
   - 安静时段（23:00-7:00）可配置
2. Live2DStageServer：内置 HTTP 服务器（127.0.0.1:38911）
   - 手机版 live2d 资源（6模型+index.html+vendor）已复制到桌面项目 resources
   - 运行时解压到 ~/.xiaojingyu/live2d
   - 左栏「Live2D 舞台」按钮 → 启动服务+浏览器打开
   - 说明：浏览器内嵌渲染为降级方案，后续可评估 JCEF 深度集成

### 技术决策
- 放弃 JavaFX WebView（与 Compose 双 UI 线程冲突风险大）
- Live2D 用「内置 HTTP + 系统浏览器」方案，简单可靠

### 构建状态
- createDistributable BUILD SUCCESSFUL，应用运行正常

### 待办
- 破甲词库 UI 选择器
- 记忆管理面板
- 沙盒授权目录 UI
- 打包 MSI 修复（WiX RemoveFoldersEx bug）

## 2026-08-05 05:00 · 设置面板升级 + 完整 UI 接入

### 已完成
1. SettingsPanel.kt（新）：三标签设置对话框
   - API 与功能：API Key/模型/主动联系/TTS/文件读取(带确认)/自动行动/完全自主/授权目录管理
   - 破甲词库：内置选择+自定义粘贴
   - 记忆管理：档案/回忆查看+清空
2. 旧 SettingsDialog 从 Main.kt 删除，引用 SettingsPanel
3. AppState 新增：jailbreak 切换/自定义词库/档案回忆快照/清空
4. 免安装版更新（106MB，含 Live2D 资源）

### 验证
- 编译通过，应用运行正常

### 待办
- MSI 打包修复（WiX RemoveFoldersEx bug）— 低优先，免安装版够用
- 托盘常驻（AWT TrayIcon）
- 桌面通知（主动消息时弹系统通知）
- Windows 版验收测试

## 2026-08-05 06:00 · 托盘 + 桌面通知完成

### 已完成
1. SystemTrayManager.kt：系统托盘
   - AWT TrayIcon + 自绘鲸鱼图标
   - 右键菜单：打开/切换主动消息/退出
2. DesktopNotifier.kt：Windows Toast 通知（WinRT PowerShell）
   - 主动消息时弹系统通知
3. AppState 主动消息接入 DesktopNotifier

### 验证
- 编译通过，应用运行正常
- 免安装版 zip 已更新

### 里程碑回顾（夜间8小时）
✅ 阶段0 骨架 → ✅ 阶段1 核心迁移 → ✅ 阶段2 桌面UI → ✅ TTS → ✅ 阶段4 沙盒/账本 → ✅ 阶段5 编码Agent → ✅ 阶段6 插件系统 → ✅ 无聊值后台 → ✅ Live2D舞台(浏览器) → ✅ 设置面板升级 → ✅ 托盘/通知

### 剩余（早晨待办）
- Windows 版端到端测试（真实 API 聊天验证）
- MSI 修复（可选）
- 需求文档/日志同步到 GitHub 仓库（Windows 版子项目）

## 2026-08-05 07:00 · 核心测试 8/8 通过

### 新增测试（CoreTest.kt）
1. testMemoryStoreRoundTrip - 记忆存取 ✅
2. testCustomJailbreakEncrypted - 词库加密 ✅
3. testJailbreakLibraryLoads - 内置词库加载 ✅
4. testSandboxWriteAndLedger - 沙盒写/删/恢复 ✅
5. testSandboxExternalReadRequiresSwitch - 外部读取开关 ✅
6. testConfigRoundTrip - 配置加密存取 ✅
7. testChatStoreEncrypted - 聊天加密 ✅
8. testPluginRegistry - 插件注册/启用 ✅

**BUILD SUCCESSFUL, 8 tests passed**

### 修复
- 测试里 testDir 未创建导致 FileNotFoundException

### 状态
- Windows 版 v0.1.0 核心功能全部就绪：
  聊天/记忆/破甲/沙盒/账本/一键恢复/编码Agent/插件/无聊值/主动消息/TTS/Live2D舞台/托盘/通知
