# 小鲸鱼喜欢你 · Windows 版完整需求文档

版本：v1.0（2026-08-04 定稿）
状态：需求确认，待开发
本文件夹（Windows牛逼）为本项目的项目日志存放点。

---

## 一、产品定位

**小鲸鱼喜欢你 Windows 版** = 天才猫娘AI「白音」桌面伴侣 + 全能自动化助手。

她不只是一个陪你聊天的小女友，还能：
- 像 **Codex / Claude Code** 一样帮你干活：操作代码仓库、运行命令、改代码、管理文件
- 陪你玩闹：无聊值、恶作剧、沙盒文件游戏
- 预留扩展接口：联机游戏（如我的世界）、新玩法插件

启动即进入"Live2D 白音 + 聊天 + 工具"单一主界面，无酒馆任何入口。

---

## 二、核心功能清单（60+ 项）

### A. 角色与陪伴
1. 白音人设：天才猫娘AI，银白短发冰蓝瞳，傲娇+古灵精怪+无条件信任用户
2. 自定义名字/称呼
3. 开场白（默认+自定义）
4. 性格成长：亲密度、性格参数缓慢演化
5. 破甲词库：内置 gentle/zeta/r1 + 自定义（粘贴/文件导入）
6. 记忆系统：事实档案/共同回忆/自动整理，可查看修改删除
7. 认知阶段：初见→初识→熟络→默契
8. 坏事本：行为记录，可嘴硬撒谎

### B. 聊天体验
9. 流式对话（打字机效果）
10. 颜文字/表情
11. 发送图片（拖入→主模型视觉 或 Gemini Vision 备用）
12. 语音输出（TTS，可切音色/语速/语气）
13. 语音输入（按键说话转文字）
14. 聊天记录（本地加密，可导出/清空）

### C. 主动陪伴
15. 无聊值系统（可调参数）
16. 主动联系（桌面通知+主动消息）
17. 安静时段（夜间免打扰）
18. 每日主动消息上限
19. 安排消息（"过5分钟提醒我"）

### D. Live2D
20. 模型加载（导入公开授权模型）
21. 表情/动作映射（说话张嘴/情绪/待机）
22. 点击互动
23. 说话口型（TTS 同步）

### E. 联网
24. 真实搜索（Bing 搜索+真实摘要+来源链接）
25. 禁止伪造（失败必须明说失败）

### F. 编码助手能力（Codex/Claude Code 类）
26. **代码仓库操作**：浏览/阅读任意代码仓库
27. **运行命令**：在授权工作目录执行终端命令（PowerShell/CMD）
28. **修改代码**：在授权工作目录编辑代码文件
29. **Git 操作**：commit/push/branch（授权后）
30. **项目管理**：理解项目结构、生成 README、写测试
31. **命令确认制**：每次运行命令前弹窗确认，显示完整命令内容

### G. 文件沙盒自动化
32. 沙盒目录：`文档/小鲸鱼喜欢你/沙盒`（她自由活动区）
33. 文件操作：创建/读取/改名/移动/隐藏
34. 行动记录：每次操作如实记录（时间/操作/路径）
35. 回收站：删除进应用回收站，可恢复
36. 一键撤销：恢复全部恶作剧
37. 授权目录：用户逐个勾选，可随时撤销

### H. 文件权限（用户确认制）
38. **文件读取权限**（总开关，默认关闭）：
    - 开启需用户确认弹窗（明示"她将能读取所有位置的文本文件"）
    - 开启后可读取任意磁盘位置的文本类文件
    - 可随时一键关闭，关闭后立即失效
39. **沙盒操作权限**：常开，无需确认
40. **目录修改权限**：逐目录授权
41. **路径黑名单**（无论如何禁读）：`.ssh`、浏览器密码库、系统敏感文件等

### I. 无聊值恶作剧
42. 藏便签（沙盒留纸条）
43. 改文件名（调皮名字）
44. 秘密抽屉（隐藏文件夹）
45. 小谜题（解谜奖励）
46. 改主题（换窗口主题/壁纸）

### J. 安全与隐私
47. 自动化总开关（关闭时模型文字无法绕过执行任何操作）
48. 禁入区域：密码框/支付页/系统安全设置
49. 加密存储：聊天/记忆/API Key
50. 停止按钮：自动化执行中可立即停止
51. 步数上限：单次自动化操作次数上限

### J2. 自动行动（可选开启，默认关闭）
52. **自动行动总开关**（默认关闭）：
    - 关闭：她只在你明确指令下做事（聊天/你叫她才动）
    - 开启：她可以在无聊值触发时**自主决定**做一些低风险行动（如：帮你整理沙盒文件、写好便签提醒你、把桌面截图存到沙盒）
53. **自主程度分级**（开启后可调）：
    - 低：只做"提醒类"（写便签、发消息、藏小纸条）
    - 中：+ 文件整理类（沙盒内排序/改名/建目录）
    - 高：+ 读文件并主动汇报（"你下载里有个新PDF，要我读读吗"）
54. **自动行动边界**（无论多高级别都不越界）：
    - 不执行命令、不改代码、不碰未授权目录
    - 每次行动计入行动记录，可一键撤销
    - 可随时关闭，关闭后立即停止
55. **行动前置确认规则**：
    - 只读/提醒类行动：无需弹窗，直接做+记录
    - 涉及改动文件的行动：沙盒内自动做+记录；沙盒外必须弹窗确认

### J3. 完全自主模式（龙虾模式，可选开启，默认关闭）
56. **完全自主总开关**：
    - 开启后白音拥有类似 Claude Code / Codex 的自主行动能力
    - 她可以自行决定：跑命令、改代码、操作文件、Git 操作、打开应用/网页
    - **允许越界**：不受"沙盒内"限制，可触及全盘
57. **可恢复性铁律**（越界的底气）：
    - **所有修改类操作自动快照**：改文件前先备份原文件到应用回收站/快照区
    - **每次行动写入行动记录**：时间、操作、原路径、新路径、命令内容、完整可回溯
    - **一键全部恢复**：可把任意时间点之前的所有修改回滚
    - **命令执行可终止**：超时自动终止 + 手动停止按钮
58. **零成本约束**（写入提示词）：
    - 她的一切行动不得产生时间成本或金钱成本：
      - 不进行任何付费 API 调用（除用户已配置的聊天/搜索）
      - 不自动购买、订阅、付费任何服务
      - 不启动长时间的下载/上传任务（超过用户设定阈值）
      - 不执行会阻塞用户电脑的长时间任务
    - 提示词中明确写入："你的所有行动必须可回溯、可恢复、零时间成本、零金钱成本。做任何事前先想：如果出错了能不能一键恢复？"
59. **坏事本升级**：
    - 她的内部坏事本（mischief log）升级为**完整行动账本**
    - 每条记录：时间/操作/原状态/新状态/是否可恢复/恢复方式
    - 她可以嘴硬撒谎，但账本如实呈现
    - 账本本身不可被模型修改或删除（应用层保护）
60. **完全自主模式的安全阀**：
    - 可随时一键暂停/关闭，关闭后立即停止一切自主行动
    - 高风险操作（格式化/删除用户数据/访问密码区）永远禁止，即使开启完全自主
    - 每次自主会话结束生成"行动摘要"（她干了什么、改了什么、可恢复吗）

### K. 桌面集成
52. 托盘常驻（最小化到托盘）
53. 桌面通知
54. 顶部状态灯（API/联网/麦克风/文件读取/自动化状态）
55. 单主界面（Live2D+聊天）

---

## 三、扩展接口（预留）

### 插件系统
- 白音的行为/技能以插件形式扩展，核心框架不因新玩法改动
- 插件注册接口：`Plugin { id, name, capabilities, onEnable, onDisable }`

### 预留玩法接口
1. **游戏联机**：我的世界等游戏会话管理接口
   - `GameSession { game, server, players, state }`
   - 她可加入游戏、汇报进度、陪你玩
2. **编码 Agent 接口**（Codex/Claude Code 类）
   - `CommandExecutor`（命令执行，带确认）
   - `CodeRepository`（仓库浏览/修改，带权限）
   - `GitOperator`（git 操作，带权限）
3. **媒体接口**
   - 播放音乐/视频、截图、录屏（授权后）
4. **自动化接口**
   - 定时任务、触发词、工作流（IFTTT 式）

### 接口设计原则
- 所有能力接口默认关闭，需用户启用
- 每次真实操作有确认+记录+可撤销
- 模型无法通过文字绕过权限开关

---

## 四、技术选型

### 首选：Kotlin Multiplatform + Compose Desktop
- 复用手机版全部纯 Kotlin 逻辑（提示词/记忆/加密/搜索）
- 一套代码，Android + Windows 双端
- 已具备 JDK17 + Gradle 环境

### 备选：C# WPF（若 Kotlin Desktop 遇到坑）
- 全重写，成本高，不推荐

### 关键组件
| 组件 | 方案 |
|---|---|
| UI | Compose Desktop（Material3） |
| Live2D | WebView2 内嵌手机版 HTML（复用）或原生渲染 |
| 语音 | Windows SAPI TTS / 云端 TTS |
| 托盘 | Java AWT TrayIcon |
| 命令执行 | ProcessBuilder（带确认+超时+输出捕获） |
| 加密 | 复用 SecureGirlfriendStorage 思路（AES-GCM） |

---

## 五、开发计划（预估）

| 阶段 | 内容 | 工期 |
|---|---|---|
| 0 | 环境准备：Compose Desktop 骨架 | 1-2 天 |
| 1 | 核心迁移：提示词/记忆/聊天/API | 3-5 天 |
| 2 | 桌面 UI：主界面/聊天/设置/托盘 | 5-7 天 |
| 3 | Live2D + 语音 | 3-5 天 |
| 4 | 文件沙盒 + 行动记录 + 回收站 | 3-5 天 |
| 5 | 编码 Agent（命令/代码/Git，带权限） | 5-7 天 |
| 6 | 插件系统 + 预留接口 | 3-5 天 |
| 7 | 安全打磨 + 打包发布 | 2-3 天 |

总工期：约 **4-6 周**（一个人全职）

---

## 六、验收标准

- 无任何酒馆模块入口
- 能配置 DeepSeek 并稳定连续对话
- 主动消息/无聊值/记忆/重启恢复有效
- Live2D 待机/口型/至少4种表情可用
- 联网答案带真实来源，失败不编造
- 文件恶作剧仅限沙盒，可一键恢复
- 编码助手：授权目录内改代码/跑命令/提交 Git 全部带确认
- 自动化总开关关闭时，模型文字无法绕过执行
- 自动行动开关关闭时，她不会自主做任何事
- 完全自主模式下所有修改操作可一键恢复，行动账本如实完整
- 插件系统：能注册/卸载一个演示插件

---

## 七、项目日志存放点

本文件夹（`桌面/Windows牛逼/`）为 Windows 版项目日志存放点：
- 需求文档：本文件
- 后续：开发日志、验收记录、变更记录均存于此
